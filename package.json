{
  "name": "talent-rediscovery",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.11.0",
  "engines": {
    "node": ">=24",
    "pnpm": ">=10"
  },
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "check": "node --check scripts/dev-server.js && node --check src/csv-candidate-records.js && node --check src/prototype-shell.js && node --check scripts/check-csv-parser.js",
    "check:csv": "node scripts/check-csv-parser.js"
  }
}
